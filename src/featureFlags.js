/**
 * Here.news Feature Configuration
 * 
 * This file contains configuration flags for enabling/disabling features.
 * These can be modified during runtime or before deployment.
 */

// Feature flags
const featureFlags = {
  // When true, shows the temporary "under construction" homepage
  // When false, shows the regular full homepage
  useTemporaryHomepage: true,
};

export default featureFlags;